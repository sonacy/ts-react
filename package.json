{
  "name": "ts-react",
  "private": true,
  "scripts": {
    "build": "lerna run build --scope={@ts-react/fiber,@ts-react/react,@ts-react/scheduler,@ts-react/shared}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": []
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  }
}